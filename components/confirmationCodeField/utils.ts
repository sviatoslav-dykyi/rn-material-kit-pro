import { VerificationMode } from "../../screens/signUp/types";

// export const handleResetCode = ({verificationMode}) => () => {
//   if (verificationMode === "email") {
//     const response = awaut resendEmailCode()
//   }
// };
